version: "3.8"

services:
  geminifleet:
    build: .
    ports:
      - "${PORT:-8000}:8000"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - PORT=8000
      - NUM_ROBOTS=${NUM_ROBOTS:-4}
    restart: unless-stopped
